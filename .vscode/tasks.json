{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build main",
            "type": "shell",
            "command": "C:/msys64/ucrt64/bin/g++.exe",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "args": [
                "-std=c++17",
                "-g",
                "main.cpp",
                "GridGenerator.cpp",
                "-o",
                "main.exe"
            ],
            "group": "build",
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Compiles the grid generator"
        }
    ]
}